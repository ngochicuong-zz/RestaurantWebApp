function PageMangement() {
	this.pages = {};
	this.activePage;
}
/*
 * page format: {name: , page: }
 * pages format : {}
 * */
PageMangement.prototype.registerPage = function(page){
	if (pages[page.name] != null) return;
	pages[page.name] = page;
}

PageMangement.prototype.actived = function(pageName){
	if (pages[pageName] == null) return;
	this.activePage = pages[pageName];
}

PageMangement.prototype.getActivePage = function(){
	return this.activePage;
}
